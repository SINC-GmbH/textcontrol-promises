declare namespace TXTextControl {
    /** Possible clipboard modes. */
    enum ClipboardMode {
        /** Specifies client clipboard mode (content is copied to and from the client clipboard). */
        Client,
        /** Specifies server clipboard mode (content is copied to and from the server clipboard). */
        Server
    }
}	