declare namespace TXTextControl {
    /** Possible types of text fields. */
    enum TextFieldType {
        /** The field type is ApplicationField. */
        ApplicationField,
        /** The field type is CheckFormField. */
        CheckFormField,
        /** The field type is DateFormField. */
        DateFormField,
        /** All FormField types. */
        FormFields,
        /** The field type is HypertextLink. */
        HypertextLink,
        /** The field type is Documentlink. */
        DocumentLink,
        /** The field type is PageNumberField. */
        PageNumberField,
        /** The field type is SelelctionFormField. */
        SelectionFormField,
        /** The field type is TextFormField. */
        TextFormField,
        /** The field type is TextField. */
        TextField,
        /** All types of text fields. */
        All
    }
}