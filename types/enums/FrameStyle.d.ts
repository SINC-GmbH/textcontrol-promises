declare namespace TXTextControl {
    /** Determines the style of a frame of an image, text frame, chart, barcode or drawing. */
    enum FrameStyle {
        /** The frame is drawn with a single line. */
        Single,
        /** The frame is drawn with a doubled line. */
        Double
    }
}