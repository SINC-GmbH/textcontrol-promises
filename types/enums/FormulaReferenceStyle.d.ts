declare namespace TXTextControl {
    /** Specifies the style of references to table cells in formulas. */
    enum FormulaReferenceStyle {
        /** A table cell in formulas is addressed with a column letter and a row number */
        A1,
        /** A table cell in formulas is addressed with a column number and a row number. */
        R1C1
    }
}