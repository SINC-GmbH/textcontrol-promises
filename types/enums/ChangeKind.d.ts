declare namespace TXTextControl {
    /** Specifies the kind of a tracked change. */
    enum ChangeKind {
        /** Text has been inserted. */
        InsertedText,
        /** Text has been deleted. */
        DeletedText
    }
}	