declare namespace TXTextControl {
    /** Information about a table. */
    interface TableInfo {
        /** The table's id. */
        id: number;
        /** The table's nesting level. */
        nestedLevel: number;
    }
}