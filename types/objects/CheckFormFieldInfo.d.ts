declare namespace TXTextControl {
    /** Information about a check form field. */
    interface CheckFormFieldInfo extends TextFieldInfo {
        /** The checked state of the check box. */
        checked: boolean;
    }
}