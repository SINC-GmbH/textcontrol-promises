declare namespace TXTextControl {
    /** Information about a signature image. */
    interface SignatureImageInfo {
        /** Maximum resolution. */
        exportMaxResolution: number;
        /** compression quality. */
        exportCompressionQuality: number;
    }
}