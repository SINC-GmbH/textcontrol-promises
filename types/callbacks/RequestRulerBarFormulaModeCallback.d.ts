declare namespace TXTextControl {
    /** Callback function for requests expectiong a RulerBarFormulaMode value. */
    type RequestRulerBarFormulaModeCallback = (formulaMode: RulerBarFormulaMode) => void;
}