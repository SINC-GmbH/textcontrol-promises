declare namespace TXTextControl {
    /** Callback function for requests expecting a horizontal alignment value. */
    type RequestHorizontalAlignmentCallback = (alignment: HorizontalAlignment) => void;
}