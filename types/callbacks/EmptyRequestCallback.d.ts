declare namespace TXTextControl {
    /** Callback function for empty requests. Notifies the caller about the completion of an operation that does not return a value. */
    type EmptyRequestCallback = () => void;
}