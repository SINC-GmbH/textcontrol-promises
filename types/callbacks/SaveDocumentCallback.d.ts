declare namespace TXTextControl {
    /** Callback function receiving the document data when saving a document or a selection. */
    type SaveDocumentCallback = (result: SaveDocumentResult) => void;
}