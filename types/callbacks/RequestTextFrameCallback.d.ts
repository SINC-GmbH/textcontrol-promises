declare namespace TXTextControl {
    /** Callback function for requests creating and adding a new TextFrame object to a text frame collection. */
    type RequestTextFrameCallback = (textFrame: TextFrame) => void;
}