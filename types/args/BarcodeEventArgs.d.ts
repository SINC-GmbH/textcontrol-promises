declare namespace TXTextControl {
    /** The event argument for barcode related events. */
    interface BarcodeEventArgs {
        /** Information about the barcode's frame. */
        barcodeFrame: FrameInfo;
    }
}