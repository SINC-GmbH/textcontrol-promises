declare namespace TXTextControl {
    /** Event argument for comment related events. */
    interface CommentEventArgs {
        /** Information about the comment. */
        commentedText: CommentedTextInfo;
    }
}