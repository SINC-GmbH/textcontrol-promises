declare namespace TXTextControl {
    /** The event argument for a tracked change related event. */
    interface TrackedChangeEventArgs {
        /** The tracked change's information. */
        trackedChange: TrackedChangeInfo;
    }
}