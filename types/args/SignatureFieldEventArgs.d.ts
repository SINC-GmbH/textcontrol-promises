declare namespace TXTextControl {
    /** The event argument for signature field related events. */
    interface SignatureFieldEventArgs {
        /** Information about the signature field. */
        signatureField: SignatureFieldInfo;
    }
}