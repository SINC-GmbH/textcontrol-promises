/** Determines the alignment of the barcode text and additional text that is displayed inside the control. */
export enum BarcodeTextAlignment {
    /** The text is displayed below the barcode image. */
    Bottom,
    /** The text is displayed above the barcode image. */
    Top,
}
