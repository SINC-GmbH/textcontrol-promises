/** Possible built-in HTML based dialog box types. */
export enum HtmlDialogBoxKind {
    /** General options. */
    OptionsDialog,
    /** Spell checking. */
    SpellCheckDialog,
    /** Loading dictionaries. */
    LoadDictionaryDialog,
    /** Accessing the thesaurus. */
    ThesaurusDialog,
    /** Loading thesaurus files. */
    LoadThesaurusDialog,
    /** Add a user. */
    PermissionsAddUserDialog,
    /** Insert or edit a bookmark. */
    InsertBookmarkDialog,
    /** Insert or edit a hyperlink. */
    HyperlinkDialog,
}
