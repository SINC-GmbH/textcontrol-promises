/** Specifies the position where to split the table. */
export enum TableAddPosition {
    /** The table is split in front of the row with the current input position. */
    Before,
    /** The table is split behind the row with the current input position. */
    After,
}
