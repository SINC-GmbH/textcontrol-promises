/** Specifies the kind of file types. */
export enum FileType {
    /** Specifies document file types. */
    Document,
    /** Specifies image file types. */
    Image,
    /** Specifies database file types. */
    DataBase,
}
