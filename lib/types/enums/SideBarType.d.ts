/** Specifies the existing side bar types. */
export enum SideBarType {
    /** No side bar. */
    None,
    /** The "Track Changes" side bar. */
    TrackChanges,
    /** The "Field Navigator" side bar. */
    FieldNavigator,
    /** The "Comments" side bar. */
    Comments,
    /** All possible side bar types. */
    All,
}
