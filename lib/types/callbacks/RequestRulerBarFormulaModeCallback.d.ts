import { RulerBarFormulaMode } from "../enums";

/** Callback function for requests expectiong a RulerBarFormulaMode value. */
export type RequestRulerBarFormulaModeCallback = (formulaMode: RulerBarFormulaMode) => void;
