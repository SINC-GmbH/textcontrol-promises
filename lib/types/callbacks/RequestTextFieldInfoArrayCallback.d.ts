import { TextFieldInfo } from "../objects";

/** Callback function for requests retrieving an array of TextFieldInfo objects. */
export type RequestTextFieldInfoArrayCallback = (textFields: TextFieldInfo[]) => void;
