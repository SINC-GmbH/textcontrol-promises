import { ReconnectionFailedEventArgs } from '../args';

/** Callback function for the reconnectionFailed event. */
export type ReconnectionFailedCallback = (args: ReconnectionFailedEventArgs) => void;
