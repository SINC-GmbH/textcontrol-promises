import { ApplicationField } from "../objects";

/** Callback function for requests expecting an ApplicationField value. */
export type RequestApplicationFieldCallback = (field: ApplicationField) => void;
