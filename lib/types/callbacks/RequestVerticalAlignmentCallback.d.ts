import { VerticalAlignment } from "../enums";

/** Callback function for requests expecting a vertical alignment value. */
export type RequestVerticalAlignmentCallback = (alignment: VerticalAlignment) => void;
