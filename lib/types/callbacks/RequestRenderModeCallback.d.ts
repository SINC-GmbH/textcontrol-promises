import { ComponentRenderMode } from "../enums";

/** Callback function for requests expecting a ComponentRenderMode value. */
export type RequestRenderModeCallback = (renderMode: ComponentRenderMode) => void;
