import { SelectionFormField } from "../objects";

/** Callback function for requesting a SelectionFormField. */
export type RequestSelectionFormFieldCallback = (selectionFormField: SelectionFormField) => void;
