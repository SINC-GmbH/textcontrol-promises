import { StatusBarBorderStyle } from '../enums';

/** Callback function that receives a status bar border style. */
export type RequestStatusBarBorderStyleCallback = (borderStyle: StatusBarBorderStyle) => void;
