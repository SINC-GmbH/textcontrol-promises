import { FrameBase } from "../objects";

/** Callback function for requests retrieving a FrameBase object from a frame collection. */
export type RequestFrameBaseCallback = (frame: FrameBase) => void;
