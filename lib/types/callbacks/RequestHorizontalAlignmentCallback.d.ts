import { HorizontalAlignment } from "../enums";

/** Callback function for requests expecting a horizontal alignment value. */
export type RequestHorizontalAlignmentCallback = (alignment: HorizontalAlignment) => void;
