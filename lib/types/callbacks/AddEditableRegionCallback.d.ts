import { EditableRegionCallbackData } from "../args";

/** Callback function for functions which add an editable region. */
export type AddEditableRegionCallback = (response: EditableRegionCallbackData) => void;
