import { TextFormFieldInfo } from "../objects";

/** The event argument for text form field related events. */
export interface TextFormFieldEventArgs {
    /** The text form field. */
    formField: TextFormFieldInfo;
}
