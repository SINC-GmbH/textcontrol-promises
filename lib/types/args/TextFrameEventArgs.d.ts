import { FrameInfo } from "../objects";

/** The event argument for text frame related events. */
export interface TextFrameEventArgs {
    /** The text frame's information. */
    textFrame: FrameInfo;
}
