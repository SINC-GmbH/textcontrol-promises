import { FrameInfo } from "../objects";

/** The event argument for drawing related events. */
export interface DrawingEventArgs {
    /** Information about the drawing frame. */
    drawingFrame: FrameInfo;
}
