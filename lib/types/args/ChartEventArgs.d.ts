import { FrameInfo } from "../objects";

/** The event argument for chart related events. */
export interface ChartEventArgs {
    /** Information about the chart's frame. */
    chartFrame: FrameInfo;
}
