import { SelectionFormFieldInfo } from "../objects";

/** The event argument for selection form field related events. */
export interface SelectionFormFieldEventArgs {
    /** The selection form field. */
    formField: SelectionFormFieldInfo;
}
