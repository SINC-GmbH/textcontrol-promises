import { TextFieldInfo } from "../objects";

/** The event argument object for text field related events. */
export interface TextFieldEventArgs {
    /** The text field. */
    textField: TextFieldInfo;
}
