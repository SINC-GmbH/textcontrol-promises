import { DateFormFieldInfo } from "../objects";

/** The event argument for date form field related events. */
export interface DateFormFieldEventArgs {
    /** The date form field. */
    formField: DateFormFieldInfo;
}
