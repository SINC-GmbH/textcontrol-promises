import { Point } from "../objects";

/** The event argument object for the textViewLocationChanged event. */
export interface TextViewLocationChangedEventArgs {
    /** The new text view location in pixels. */
    location: Point;
}
