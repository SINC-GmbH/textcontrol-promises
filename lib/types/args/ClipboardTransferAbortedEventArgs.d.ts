/** The event argument object for the ClipboardDataTransferAbortedCallback callback. */
export interface ClipboardTransferAbortedEventArgs {
    /** Unique ID string of the aborted transfer. */
    id: string;
}
