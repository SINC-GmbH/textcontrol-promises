import { ClipboardMode } from "../enums";

/** The event argument object for the clipboardModeChanged event. */
export interface ClipboardModeChangedEventArgs {
    /** The clipboard mode. */
    clipboardMode: ClipboardMode;
}
