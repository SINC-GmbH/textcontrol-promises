import { TextCharCollectionBase } from './generated/TextCharCollectionBase.js';

// Collection methods (getCount, elementAt, forEach, async iterator) come from Collection via TextCharCollectionBase.
export class TextCharCollection extends TextCharCollectionBase {}
