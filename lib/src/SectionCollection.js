import { SectionCollectionBase } from './generated/SectionCollectionBase.js';

// Collection methods (getCount, elementAt, forEach, async iterator) come from Collection via SectionCollectionBase.
export class SectionCollection extends SectionCollectionBase {}
