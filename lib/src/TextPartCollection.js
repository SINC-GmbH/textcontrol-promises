import { TextPartCollectionBase } from './generated/TextPartCollectionBase.js';

// Collection methods (getCount, elementAt, forEach, async iterator) come from Collection via TextPartCollectionBase.
export class TextPartCollection extends TextPartCollectionBase {}
