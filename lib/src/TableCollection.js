import { TableCollectionBase } from './generated/TableCollectionBase.js';

// Collection methods (getCount, elementAt, forEach, async iterator) come from Collection via TableCollectionBase.
export class TableCollection extends TableCollectionBase {}
