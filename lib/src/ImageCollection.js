import { ImageCollectionBase } from './generated/ImageCollectionBase.js';

// Collection methods (getCount, elementAt, forEach, async iterator) come from Collection via ImageCollectionBase.
export class ImageCollection extends ImageCollectionBase {}
