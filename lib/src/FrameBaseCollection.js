import { FrameBaseCollectionBase } from './generated/FrameBaseCollectionBase.js';

// Collection methods (getCount, elementAt, forEach, async iterator) come from Collection via FrameBaseCollectionBase.
export class FrameBaseCollection extends FrameBaseCollectionBase {}
