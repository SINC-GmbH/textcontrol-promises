import { TableRowCollectionBase } from './generated/TableRowCollectionBase.js';

// Collection methods (getCount, elementAt, forEach, async iterator) come from Collection via TableRowCollectionBase.
export class TableRowCollection extends TableRowCollectionBase {}
